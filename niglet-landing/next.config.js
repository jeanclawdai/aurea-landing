/** @type {import('next').NextConfig} */
const nextConfig = {
  // Transpile reactbits-installer
  transpilePackages: ['reactbits-installer'],
  
  // Turbopack config (empty for now)
  turbopack: {},
};

module.exports = nextConfig;
