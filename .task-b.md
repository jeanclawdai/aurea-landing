
## Agent B: Typography semibold + Pricing holographic border + iPhone bigger

Working dir: /Users/jeanclawd/.openclaw/workspace/aurea-landing

---

## 1. TYPOGRAPHY — Dial down from extrabold to bold across all sections

Find every occurrence of `font-extrabold` in all .tsx files under app/ and replace with `font-bold` EXCEPT in the hero stat numbers (847%, 10K+, 10x) and pricing prices ($299, $599) which should stay heavy.

Run this to check: grep -rn "font-extrabold" app/ --include="*.tsx"

Then for each file, change section headlines from `font-extrabold` → `font-bold` (the large text-5xl+ headings).
Keep `font-extrabold` only for:
- The price numbers in Pricing.tsx
- The hero stat numbers

---

## 2. PRICING — Remove green, add holographic border to popular card

Read app/sections/Pricing.tsx fully first.

### Remove green color
Change all `text-green-600` in Pricing.tsx to:
- Starter plan price: `text-gray-950`
- Growth plan price: `text-white` 
- Studio "Custom": `text-gray-950` (stays)
- The ✓ check icons: change `text-green-500` to `text-gray-400` (light cards) and `text-gray-500` (dark card)

### Holographic border on popular card
Add this CSS to globals.css:
```css
@keyframes holographic {
  0% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
  100% { background-position: 0% 50%; }
}

.holographic-border {
  position: relative;
  z-index: 0;
}

.holographic-border::before {
  content: '';
  position: absolute;
  inset: -2px;
  border-radius: 26px; /* 24px + 2px border */
  background: linear-gradient(
    135deg,
    #ff0080, #ff8c00, #ffd700, #00ff88, #00cfff, #a855f7, #ff0080
  );
  background-size: 300% 300%;
  animation: holographic 3s ease infinite;
  z-index: -1;
  opacity: 0.85;
}

.holographic-border::after {
  content: '';
  position: absolute;
  inset: -2px;
  border-radius: 26px;
  background: linear-gradient(
    135deg,
    #ff0080, #ff8c00, #ffd700, #00ff88, #00cfff, #a855f7, #ff0080
  );
  background-size: 300% 300%;
  animation: holographic 3s ease infinite;
  z-index: -2;
  opacity: 0.3;
  filter: blur(8px);
}
```

In Pricing.tsx, add `holographic-border` class to the popular plan card:
```tsx
// popular plan card:
className={`rounded-3xl p-10 flex flex-col bg-gray-950 holographic-border`}
```

---

## 3. IPHONE — Make bigger in BeforeAfter.tsx

Read app/sections/BeforeAfter.tsx fully.

Change the iPhone width from `w-[260px]` to `w-[300px]` in the IPhoneMockup component.
Change height from `height: 520` to `height: 580`.
The phone can be cropped at the bottom — that's fine.

Also improve the avatar placeholder in the iPhone:
- Before state avatar: a nice gradient using CSS, emoji 🏥
- After state avatar: use a gradient from purple to pink with a sparkle ✨ emoji to feel "glow-up"

For the feed grid images (the 9 grid squares at the bottom):
- Before: keep `bg-gray-100` (empty/dull)
- After: make them colorful gradients:
  ```tsx
  // In the feed grid for after state:
  const afterColors = [
    "bg-gradient-to-br from-pink-200 to-rose-300",
    "bg-gradient-to-br from-purple-200 to-violet-300",
    "bg-gradient-to-br from-blue-200 to-cyan-300",
    "bg-gradient-to-br from-orange-200 to-amber-300",
    "bg-gradient-to-br from-emerald-200 to-teal-300",
    "bg-gradient-to-br from-pink-300 to-purple-400",
    "bg-gradient-to-br from-yellow-200 to-orange-300",
    "bg-gradient-to-br from-cyan-200 to-blue-300",
    "bg-gradient-to-br from-rose-200 to-pink-300",
  ];
  ```
  Then in the grid: `className={\`aspect-square rounded-sm \${isAfter ? afterColors[i] : "bg-gray-100"} flex items-center justify-center\`}`

Also add a fire emoji overlay on after grid items with higher indices:
  ```tsx
  {isAfter && i < 3 && <span className="text-[8px]">🔥</span>}
  {isAfter && i >= 3 && <span className="text-[8px]">✨</span>}
  ```

---

## COMMIT when done
git add -A && git commit -m "feat: semibold headings, holographic pricing border, bigger iPhone with colorful feed"
Then: openclaw system event --text "Done: Agent B complete" --mode now
